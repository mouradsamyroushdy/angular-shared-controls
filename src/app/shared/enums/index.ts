import { DocumentStatus } from './document-status';
import { QueueItemStatus } from './queue-item-status';
import { FieldType } from './field-type.enum';
import { RowSchemaType } from './row-schema-type.enum';

export const ALL = [
    DocumentStatus,
    FieldType,
    QueueItemStatus,
    RowSchemaType,
];

export {
    DocumentStatus,
    FieldType,
    QueueItemStatus,
    RowSchemaType,
};